/*
  have a set of cxbitarrays

  vclass-evolve 32 classifier functions

  use minigen to decide which of the 32 to use as part of an ensemble
  classifier, which is a set of votes as to the classification

  return a set of sets of 4 unsigned chars, that overall comprises the
  ensemble classifier
*/

int x;
